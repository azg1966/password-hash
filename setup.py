from setuptools import setup, find_packages


setup(
    name='PasswordHash',
    version='0.1',
    packages=find_packages(),
    author='Alexander Z.',
    description="Simple generate and check password hash functions")
